# Kiro2Api 管理后台初始密码示例
#
# 说明：
# - 管理后台（/login.html）登录密码读取自项目根目录的 `pwd` 文件。
# - `pwd` 不应提交到 git（已在 .gitignore 中忽略）。
# - 你可以先使用明文密码（例如：随机强口令），登录后再在后台“配置管理”中更新密码。
#   更新后会以 scrypt 哈希格式写入 `pwd`，更安全。
#
# 使用方式：
#   cp pwd.example pwd
#   # 然后把下面这一行替换成你的强密码（建议 >= 16 位）

CHANGE_ME_TO_A_STRONG_PASSWORD

