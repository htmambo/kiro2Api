# npm registry 配置
# 使用官方 npm registry 确保依赖一致性
registry=https://registry.npmjs.org/

# 保存精确版本（避免 lock 文件频繁变化）
save-exact=false

# 注意：不要显式关闭 optional 依赖
# Rollup、Lightning CSS 等工具会通过平台相关的 optional package 分发原生二进制
# 若关闭 optional，跨平台切换后容易出现当前平台缺包、构建失败的问题
